name: Wake Up Render
on:
  schedule:
    - cron: '*/14 * * * *' # This runs every 14 mins
  workflow_dispatch:      # Allows you to run it manually to test

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping My Site
        run: curl -I https://nlk-gcedowloader.onrender.com
